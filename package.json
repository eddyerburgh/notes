{
  "name": "notes",
  "version": "1.0.0",
  "private": true,
  "license": "MIT",
  "scripts": {
    "dev": "rm -rf _site && bundle exec jekyll serve -Ilo",
    "format": "prettier --write 'docs/**/*.md'"
  },
  "dependencies": {
    "prettier": "^1.18.2"
  }
}
